plugins {
  id "org.sonarqube" version "3.5.0.2730"
}

sonarqube {
  properties {
    property "sonar.projectKey", "Breinich_benchmarking-portal-analysis"
    property "sonar.organization", "breinich"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
